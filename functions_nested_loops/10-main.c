#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int sum;

    sum = add(3, 5);
    printf("Sum: %d\n", sum);

    return (0);
}
